# WardSeal Development Configuration
# This file is for local development only and should NOT be committed to git.
# Add this file to .gitignore

# Development secrets (local dev only - use Vault in staging/prod)
DB_PASSWORD=password
SERVICE_AUTH_TOKEN=dev-internal-token

# Optional: Set these for local development with Vault
# VAULT_ADDR=http://localhost:8200
# VAULT_TOKEN=root
