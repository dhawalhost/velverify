# External Secrets Operator - Production Configuration
# Automatically syncs secrets from HashiCorp Vault
#
# Prerequisites:
# 1. Install External Secrets Operator
# 2. Create ClusterSecretStore for Vault
# 3. Ensure Vault policies are correctly configured
---
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: vault-backend-production
spec:
  provider:
    vault:
      server: "http://vault.vault.svc.cluster.local:8200"
      path: "kv"
      version: "v2"
      auth:
        appRole:
          path: "approle"
          roleRef:
            name: wardseal-vault-approle
            key: role_id
            namespace: wardseal-production
          secretRef:
            name: wardseal-vault-approle
            key: secret_id
            namespace: wardseal-production
---
# NOTE: Bootstrap secret 'wardseal-vault-approle' must be created manually
# with real AppRole credentials from Vault. Never commit secrets to Git!
#
# Create it with:
# kubectl create secret generic wardseal-vault-approle \
#   --namespace wardseal-production \
#   --from-literal=role_id="<YOUR_ROLE_ID>" \
#   --from-literal=secret_id="<YOUR_SECRET_ID>"
---
# Database Credentials
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wardseal-db-credentials-production
  namespace: wardseal-production
spec:
  refreshInterval: 1m  # More frequent refresh in production
  secretStoreRef:
    name: vault-backend-production
    kind: ClusterSecretStore
  target:
    name: wardseal-db-credentials-production
    creationPolicy: Owner
    deletionPolicy: Retain  # Don't delete secret if ExternalSecret is deleted
  data:
    - secretKey: username
      remoteRef:
        key: secret/wardseal/production/database
        property: username
    - secretKey: password
      remoteRef:
        key: secret/wardseal/production/database
        property: password
    - secretKey: host
      remoteRef:
        key: secret/wardseal/production/database
        property: host
    - secretKey: port
      remoteRef:
        key: secret/wardseal/production/database
        property: port
    - secretKey: database
      remoteRef:
        key: secret/wardseal/production/database
        property: database
---
# Service Auth Token
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wardseal-service-auth-production
  namespace: wardseal-production
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: vault-backend-production
    kind: ClusterSecretStore
  target:
    name: wardseal-service-auth-production
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: token
      remoteRef:
        key: secret/wardseal/production/service-auth
        property: token
---
# Vault KMS Configuration
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wardseal-vault-kms-production
  namespace: wardseal-production
spec:
  refreshInterval: 30m
  secretStoreRef:
    name: vault-backend-production
    kind: ClusterSecretStore
  target:
    name: wardseal-vault-kms-production
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: VAULT_ADDR
      remoteRef:
        key: secret/wardseal/production/kms
        property: vault_addr
    - secretKey: VAULT_ROLE_ID
      remoteRef:
        key: secret/wardseal/production/kms
        property: role_id
    - secretKey: VAULT_SECRET_ID
      remoteRef:
        key: secret/wardseal/production/kms
        property: secret_id
    - secretKey: VAULT_KEY_NAME
      remoteRef:
        key: secret/wardseal/production/kms
        property: key_name
---
# License Key (Enterprise)
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wardseal-license-production
  namespace: wardseal-production
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend-production
    kind: ClusterSecretStore
  target:
    name: wardseal-license-production
    creationPolicy: Owner
  data:
    - secretKey: LICENSE_KEY
      remoteRef:
        key: secret/wardseal/production/license
        property: key
