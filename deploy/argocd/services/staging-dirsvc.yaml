apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: wardseal-staging-dirsvc
  namespace: argocd
  labels:
    environment: staging
    service: dirsvc
spec:
  project: default
  source:
    repoURL: https://github.com/dhawalhost/wardseal.git
    targetRevision: main
    path: deploy/charts/dirsvc
    helm:
      releaseName: wardseal
      valueFiles:
        - values.yaml
        - values-staging.yaml
        - version-staging.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: wardseal-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
      - ApplyOutOfSyncOnly=true
