apiVersion: v1
kind: Service
metadata:
  name: {{ include "provsvc.fullname" . }}
  labels:
    {{- include "provsvc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "provsvc.selectorLabels" . | nindent 4 }}
