apiVersion: v1
kind: Service
metadata:
  name: {{ include "govsvc.fullname" . }}
  labels:
    {{- include "govsvc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "govsvc.selectorLabels" . | nindent 4 }}
