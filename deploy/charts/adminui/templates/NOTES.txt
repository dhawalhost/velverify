Admin UI has been deployed successfully!

RELEASE NAME: {{ .Release.Name }}
NAMESPACE: {{ .Release.Namespace }}

NOTE: The Admin UI is a static SPA (nginx). API endpoint URLs (VITE_AUTHSVC_URL,
VITE_DIRSVC_URL, VITE_GOVSVC_URL) are baked into the JavaScript bundle at Docker
build time via --build-arg. To change them you must rebuild the image.

1. Get the application URL:
{{- if .Values.ingress.enabled }}
  {{- range .Values.ingress.hosts }}
  http://{{ .host }}
  {{- end }}
{{- else }}
  Run these commands to access the Admin UI:
  
  export POD_NAME=$(kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "adminui.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl port-forward $POD_NAME 3000:80 -n {{ .Release.Namespace }}
  
  Then visit: http://127.0.0.1:3000
{{- end }}

2. Check the deployment status:
  kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "adminui.name" . }}"

3. View logs:
  kubectl logs -f deployment/{{ include "adminui.fullname" . }} -n {{ .Release.Namespace }}

For more information, visit: https://github.com/dhawalhost/wardseal
