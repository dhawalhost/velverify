apiVersion: v1
kind: Service
metadata:
  name: {{ include "policysvc.fullname" . }}
  labels:
    {{- include "policysvc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "policysvc.selectorLabels" . | nindent 4 }}
